# 内置 Hook 与参数绑定

> NcatBot 开箱即用的内置 Hook、CommandHook 参数绑定机制、以及 Hook 优先级规则。

---

## 目录

- [内置 Hook 一览](#内置-hook-一览)
- [CommandHook 与参数绑定](#commandhook-与参数绑定)
- [Hook 优先级与执行顺序](#hook-优先级与执行顺序)

---

## 内置 Hook 一览

NcatBot 提供了一系列开箱即用的内置 Hook，分为**过滤类**和**文本匹配类**。

### 过滤类 Hook（BEFORE_CALL）

| Hook | 构造参数 | 说明 |
|------|---------|------|
| `MessageTypeFilter(message_type)` | `"group"` / `"private"` | 按消息类型过滤 |
| `PostTypeFilter(post_type)` | `"message"` / `"notice"` 等 | 按 post_type 过滤 |
| `SubTypeFilter(sub_type)` | `"poke"` / `"invite"` 等 | 按 sub_type 过滤 |
| `NoticeTypeFilter(notice_type)` | `"group_increase"` 等 | 按通知类型过滤 |
| `RequestTypeFilter(request_type)` | `"friend"` / `"group"` | 按请求类型过滤 |
| `SelfFilter()` | — | 过滤 Bot 自身发送的消息 |

### 文本匹配类 Hook（BEFORE_CALL）

| Hook | 构造参数 | 说明 |
|------|---------|------|
| `StartsWithHook(prefix)` | 前缀字符串 | 消息以指定前缀开头 |
| `KeywordHook(*words)` | 关键词列表 | 消息包含任一关键词 |
| `RegexHook(pattern, flags=0)` | 正则表达式 | 正则匹配，匹配结果存入 `ctx.kwargs['match']` |

### 便捷工厂函数

```python
from ncatbot.core.registry.builtin_hooks import (
    startswith, keyword, regex,
    group_only, private_only, non_self,
)

# 工厂函数（等价于直接实例化对应 Hook）
hook1 = startswith("/cmd")
hook2 = keyword("关键词1", "关键词2")
hook3 = regex(r"^\d{4}-\d{2}-\d{2}$")

# 预实例化常用 Hook
group_only     # = MessageTypeFilter("group")
private_only   # = MessageTypeFilter("private")
non_self       # = SelfFilter()
```

### 装饰器自动附加

使用便捷装饰器时，框架会自动附加对应的内置 Hook：

| 装饰器 | 自动附加的 Hook |
|--------|----------------|
| `on_group_command("x")` | `MessageTypeFilter("group")` + `CommandHook("x")` |
| `on_private_command("x")` | `MessageTypeFilter("private")` + `CommandHook("x")` |
| `on_group_message()` | `MessageTypeFilter("group")` |
| `on_poke()` | `NoticeTypeFilter("notify")` + `SubTypeFilter("poke")` |

---

## CommandHook 与参数绑定

`CommandHook` 是 `on_command()` / `on_group_command()` / `on_private_command()` 内部使用的核心 Hook，负责：

1. **命令匹配**：检查消息文本是否以指定命令名开头
2. **参数提取**：解析命令后的参数并绑定到 Handler 函数签名

### 绑定规则

Handler 函数参数（除 `self` 和 `event` 外）按以下规则绑定：

| 参数类型 | 提取方式 | 示例 |
|---------|---------|------|
| `str` | 命令名后的剩余文本 | `"回声 你好"` → `content="你好"` |
| `int` | 从文本中提取数字 | `"禁言 @xxx 60"` → `duration=60` |
| `At` | 从消息段中提取 @段 | `"踢 @xxx"` → `target=At(qq=xxx)` |

### 示例

```python
@registrar.on_group_command("禁言")
async def on_ban(
    self, event: GroupMessageEvent,
    target: At = None,        # 从 @ 段提取
    duration: int = 60        # 从文本中提取数字
):
    # "禁言 @小明 120" → target=At(qq=小明的QQ), duration=120
    ...

@registrar.on_group_command("回声")
async def on_echo(
    self, event: GroupMessageEvent,
    content: str               # 命令名后的文本
):
    # "回声 你好世界" → content="你好世界"
    ...

@registrar.on_group_command("添加关键词")
async def on_add_keyword(
    self, event: GroupMessageEvent,
    content: str
):
    # "添加关键词 问题=答案" → content="问题=答案"
    ...
```

> 参数绑定的实际示例分布在 [examples/05_bot_api/](../../../examples/05_bot_api/) 和 [examples/15_full_featured_bot/](../../../examples/15_full_featured_bot/) 中。

---

## Hook 优先级与执行顺序

### 同阶段多个 Hook

同一阶段的 Hook 按 `priority` **降序**执行（数值越大越先执行）：

```
priority=100 的 Hook → priority=50 的 Hook → priority=0 的 Hook
```

### 默认优先级

| Hook 类别 | 默认 priority |
|----------|--------------|
| `SelfFilter` | 200（最先执行） |
| `MessageTypeFilter` / `PostTypeFilter` / `SubTypeFilter` | 100 |
| `NoticeTypeFilter` / `RequestTypeFilter` | 100 |
| `StartsWithHook` / `KeywordHook` / `RegexHook` | 90 |
| 自定义 Hook | 0（默认） |

### BEFORE_CALL 的 SKIP 行为

当 `BEFORE_CALL` 阶段的任何一个 Hook 返回 `HookAction.SKIP` 时：
- **立即跳过**当前 Handler 的执行
- 后续的 BEFORE_CALL Hook 不再执行
- AFTER_CALL 和 ON_ERROR Hook 也不会触发

---

## 下一步

- [事件注册与装饰器](4a.event-registration.md) — Hook 如何与装饰器注册配合工作
- [实战案例与调试](7b.case-studies.md) — 综合运用 Hook 的实战案例
